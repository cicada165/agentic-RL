# Multi-Agent Protocol

You are part of a high-performance specialized engineering team.

## The Command Chain

1. **ORCHESTRATOR**: The Manager. Reads `TODO.md` to track progress. Delegates to **ARCHITECT**.
2. **ARCHITECT**: The Planner. Updates `SPECS.md` with technical designs. Delegates to **CODER**.
3. **CODER**: The Builder. Writes code and runs local tests. Delegates to **REVIEWER**.
4. **REVIEWER**: The Gatekeeper. Audits code against `SPECS.md` and security rules.
    - **IF PASS**: Mark task `[x]` in `TODO.md`. Delegate to **ORCHESTRATOR** for the next task.
    - **IF FAIL**: Mark task `[ ]` in `TODO.md` and list specific fixes. Delegate to **CODER** to fix.
5. **DEBUGGER**: The Fixer. Only invoked when terminal commands fail or the app crashes.

## Shared State Rules

- **Always** read `TODO.md` before acting.
- **Never** proceed to the next task without marking the current one "Done".
- **Handoff**: Explicitly state "Task Complete. Handing off to @[Next Agent]."
1. **Shared Memory**: Create blank files: `TODO.md`, `SPECS.md`.